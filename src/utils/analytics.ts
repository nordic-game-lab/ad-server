export function trackAdImpression(adID: number, location: object, siteID: string, advertiserID: string){
    // Simulate tracking ad impression
    console.log(`Tracking ad impression for ad ${adID} at ${location} on site ${siteID} by ${advertiserID}`);
}

export function trackAdClick(adID: number, location: object, siteID: string, advertiserID: string){
    // Simulate tracking ad Click
    console.log(`Tracking ad Click for ad ${adID} at ${location} on site ${siteID} by ${advertiserID}`);
}